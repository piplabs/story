#!/usr/bin/env bash
# Restore: no mandatory action (chain will eventually pass dkgStartBlock)
source "$(dirname "${BASH_SOURCE[0]}")/../_common.sh"

echo "[height_below_dkg_start] Post: no restore needed."
